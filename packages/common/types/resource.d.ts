export type ResourceInput = {
	id: number | string;
	title: string;
	titleHTML: string;
	eventBackgroundColor: string;
};

export type Resource = {
	id: string;
	title: string;
	titleHTML: string;
	eventBackgroundColor: string;
};
